<component-spec-template>

<purpose>
Template and guidance for generating component specifications from React/Vue/Svelte components.
Used by /sensei:generate-specs to create component documentation.
</purpose>

<detection_patterns>
## Component Detection Patterns

### React (Function Components)
```typescript
export function Button(props: ButtonProps) { ... }
export const Button: React.FC<ButtonProps> = (props) => { ... }
export default function Button({ label, onClick }: ButtonProps) { ... }
```

### React (Class Components)
```typescript
export class Button extends React.Component<ButtonProps> { ... }
```

### Vue 3 (Composition API)
```vue
<script setup lang="ts">
defineProps<{ label: string; disabled?: boolean }>()
defineEmits<{ click: [event: MouseEvent] }>()
</script>
```

### Vue 3 (Options API)
```typescript
export default defineComponent({
  props: { label: String, disabled: Boolean },
  emits: ['click']
})
```

### Svelte
```svelte
<script lang="ts">
  export let label: string;
  export let disabled = false;
</script>
```
</detection_patterns>

<extraction_rules>
## Information Extraction

### Props/Properties
- Name and type
- Required vs optional
- Default values
- Description (from JSDoc/comments)
- Validation constraints

### Events/Emits
- Event name
- Payload type
- When triggered

### Slots (Vue/Svelte)
- Slot name
- Slot props
- Default content

### State (Internal)
- State variables
- Their purpose

### Refs/Exposed Methods
- Ref name
- Exposed functions
- Usage pattern

### Styling
- CSS custom properties (--var)
- CSS classes exposed
- Tailwind variants
</extraction_rules>

<output_template>
## Component Specification Template

```markdown
# {ComponentName}

> {One-line description of what this component does}

**Location:** `{file_path}`
**Category:** {UI | Layout | Form | Data Display | Feedback | Navigation}
**Status:** {Stable | Beta | Deprecated}

## Overview

{2-3 sentence description of the component's purpose and when to use it}

## Preview

<!-- If storybook or similar exists -->
[Storybook]({storybook_url}) | [Figma]({figma_url})

## Installation

```tsx
import { {ComponentName} } from '{import_path}';
```

## Props

| Prop | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| {prop_name} | `{type}` | `{default}` | {Yes/No} | {description} |

### Prop Details

#### `{complex_prop_name}`

{Detailed explanation for complex props}

```typescript
type {PropType} = {
  {field}: {type};
};
```

## Events

| Event | Payload | Description |
|-------|---------|-------------|
| {event_name} | `{payload_type}` | {when_triggered} |

## Slots

| Slot | Props | Description |
|------|-------|-------------|
| default | - | {description} |
| {slot_name} | `{props}` | {description} |

## Examples

### Basic Usage

```tsx
<{ComponentName} {required_prop}="{value}" />
```

### With All Props

```tsx
<{ComponentName}
  {prop1}="{value1}"
  {prop2}="{value2}"
  on{Event}={(e) => console.log(e)}
/>
```

### Common Patterns

#### {Pattern Name}

```tsx
{code_example}
```

## Variants

{If component has visual variants}

| Variant | Description | Example |
|---------|-------------|---------|
| {variant_name} | {description} | `<{ComponentName} variant="{variant_name}" />` |

## Accessibility

- **Role:** {ARIA role if applicable}
- **Keyboard:** {keyboard interactions}
- **Screen Reader:** {screen reader behavior}

### ARIA Attributes

| Attribute | Value | Purpose |
|-----------|-------|---------|
| {aria-attr} | {value} | {purpose} |

## Styling

### CSS Custom Properties

| Property | Default | Description |
|----------|---------|-------------|
| `--{component}-{property}` | `{value}` | {description} |

### CSS Classes

| Class | Purpose |
|-------|---------|
| `.{class-name}` | {purpose} |

## Dependencies

- {dependency}: {why_needed}

## Related Components

- [{RelatedComponent}](./{RelatedComponent}.spec.md) - {relationship}

## Changelog

| Version | Changes |
|---------|---------|
| {version} | {changes} |

---
*Auto-generated by BuildSensei. Last updated: {timestamp}*
```
</output_template>

<file_organization>
## Output File Structure

```
docs/spec/components/
├── index.md              # Component index with categories
├── ui/
│   ├── Button.spec.md
│   ├── Input.spec.md
│   └── Card.spec.md
├── layout/
│   ├── Container.spec.md
│   ├── Grid.spec.md
│   └── Stack.spec.md
├── form/
│   ├── Form.spec.md
│   ├── TextField.spec.md
│   └── Select.spec.md
├── data-display/
│   ├── Table.spec.md
│   ├── List.spec.md
│   └── Avatar.spec.md
├── feedback/
│   ├── Alert.spec.md
│   ├── Toast.spec.md
│   └── Modal.spec.md
└── navigation/
    ├── Navbar.spec.md
    ├── Sidebar.spec.md
    └── Breadcrumb.spec.md
```

### Index File Pattern

```markdown
# Component Library

## UI Components
- [Button](./ui/Button.spec.md) - Interactive button element
- [Input](./ui/Input.spec.md) - Text input field

## Layout Components
- [Container](./layout/Container.spec.md) - Content container
- [Grid](./layout/Grid.spec.md) - Grid layout system

## Form Components
...
```
</file_organization>

<type_extraction>
## TypeScript Type Extraction

### From Interface/Type
```typescript
interface ButtonProps {
  /** Button label text */
  label: string;
  /** Click handler */
  onClick?: () => void;
  /** Visual variant */
  variant?: 'primary' | 'secondary' | 'ghost';
  /** Disabled state */
  disabled?: boolean;
  /** Button size */
  size?: 'sm' | 'md' | 'lg';
}
```

Extracts to:

| Prop | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| label | `string` | - | Yes | Button label text |
| onClick | `() => void` | - | No | Click handler |
| variant | `'primary' \| 'secondary' \| 'ghost'` | `'primary'` | No | Visual variant |
| disabled | `boolean` | `false` | No | Disabled state |
| size | `'sm' \| 'md' \| 'lg'` | `'md'` | No | Button size |

### From Default Props
```typescript
Button.defaultProps = {
  variant: 'primary',
  size: 'md',
  disabled: false
};
```

### From Destructuring Defaults
```typescript
function Button({
  variant = 'primary',
  size = 'md',
  disabled = false
}: ButtonProps) { ... }
```
</type_extraction>

<categorization_rules>
## Component Categorization

### UI (Basic building blocks)
- Button, IconButton, Link
- Badge, Tag, Chip
- Avatar, Icon
- Divider, Spacer

### Layout (Structure and arrangement)
- Container, Box, Paper
- Grid, Flex, Stack
- AspectRatio, Center

### Form (User input)
- Input, Textarea, Select
- Checkbox, Radio, Switch
- DatePicker, TimePicker
- Slider, Rating
- Form, FormField, FormLabel

### Data Display (Showing information)
- Table, DataGrid
- List, Tree
- Card, Accordion
- Stat, Progress
- Code, Kbd

### Feedback (User communication)
- Alert, Toast, Snackbar
- Modal, Dialog, Drawer
- Tooltip, Popover
- Skeleton, Spinner

### Navigation (Moving around)
- Navbar, Sidebar
- Menu, Dropdown
- Tabs, Breadcrumb
- Pagination, Steps
</categorization_rules>

</component-spec-template>
